# 复制成 .dev.vars 后填入真实值（本文件是模板，会进仓库）
#
# 敏感值放这里（本地）和 Cloudflare Secret（生产）。
# 非敏感的配置放 wrangler.jsonc 的 vars，两处共用同一份。

# 管理员密码的 sha256，用 `npm run hash-password -- 你的密码` 生成。
# 必须是 64 位十六进制 —— 填明文密码会导致登录永远返回 401。
ADMIN_PASSWORD_HASH=

# 签发登录 token 的密钥。随便一串长随机值，泄露的话别人能伪造登录凭证。
SESSION_SECRET=
